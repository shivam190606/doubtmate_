# DoubtMate (MVP)


## Quickstart (Linux / macOS)


1. Create project folder and files (copy from this document).
2. Create a virtual environment:
```bash
python3 -m venv venv
source venv/bin/activate